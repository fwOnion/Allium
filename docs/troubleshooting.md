# Troubleshooting

Run setup with `-DebugTrace` and sanitize logs before sharing. Use a user-writable installation folder. Run `-Verify` to check dependencies. Use a fresh terminal after PowerShell or module changes. Corrupt `data/settings.json` is moved to a timestamped backup so defaults can be regenerated.
