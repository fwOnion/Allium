# Repository Readiness

This foundation intentionally omits root `Allium.ps1` and `Allium-Setup.ps1`. Add only the verified public minified artifact and the setup after the exact WinUIShell 0.12.0 pin is patched and verified. Run native parser, repository validation, clean-machine setup tests, and release packaging before publication.
